version: '3.8'

services:
  spring-application:
    container_name: spring
    image: ${SPRING_IMAGE}
    ports:
      - ${SPRING_OUTER_PORT}:${SPRING_INNER_PORT}
    environment:
      - SPRING_PROFILES_ACTIVE=release